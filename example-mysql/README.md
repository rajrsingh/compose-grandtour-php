# Connecting to MySQL from a PHP App

## Connection String

The connection string provided by your Compose MySQL deployment should go into the file `config.php` (created by copying `config.template.php`).

